{
  "name": "spigot",
  "version": "0.1.0",
  "private": true,
  "description": "Spigot — pay-per-call settlement rails for APIs, AI agents, and machine services on X1 EcoChain. An X1-native implementation of the x402 (HTTP 402 Payment Required) pattern, with a prepaid-credit mode for high-frequency agent callers.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network x1Testnet"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}
