{
  "name": "verinode",
  "version": "0.1.0",
  "private": true,
  "description": "Verinode — on-chain node liveness attestation for X1 EcoChain. Node operators register and submit periodic heartbeats; a public dashboard shows verifiable uptime, not self-reported claims.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network x1Testnet",
    "agent": "node scripts/agent.js"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}
