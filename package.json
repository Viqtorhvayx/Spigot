{
  "name": "tipstream",
  "version": "0.1.0",
  "private": true,
  "description": "Tipstream — creator tipping on X1 EcoChain. Every tip splits automatically on-chain: a small platform fee, the rest straight to the creator.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network x1Testnet"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5"
  }
}
